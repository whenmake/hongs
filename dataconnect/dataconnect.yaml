specVersion: "v1beta"
serviceId: "project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
